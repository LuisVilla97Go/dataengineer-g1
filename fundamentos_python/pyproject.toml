[project]
name = "fundamentos-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "LuisVilla97Go", email = "luisantoniovilla97@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
fundamentos-python = "fundamentos_python:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
